# GLM-4.7-Flash (30B MoE, ~3.6B active) via llama.cpp — Q8_K_XL quantization
#
# Higher quality 8-bit quantization. Requires ~33 GB base VRAM.
# The GGUF model is downloaded on first start (~35 GB).
# Downloads are cached in the shared glm47_huggingface_cache volume.
#
# Usage: docker compose -f docker-compose.llama-q8-rtx.yml up -d
# API endpoint: http://localhost:11346/v1

services:
  llama-server:
    image: ghcr.io/ggml-org/llama.cpp:server-cuda
    container_name: glm47-llama-q8
    restart: unless-stopped

    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

    ports:
      - "11346:11346"

    volumes:
      - model_cache:/root/.cache/huggingface

    environment:
      - LLAMA_CACHE=/root/.cache/huggingface/llama.cpp

    command:
      - --hf-repo
      - unsloth/GLM-4.7-Flash-GGUF
      - --hf-file
      - GLM-4.7-Flash-UD-Q8_K_XL.gguf
      - -ngl
      - "99"
      - -c
      - "202752"
      - -np
      - "1"
      - -fa
      - "on"
      - --cache-type-k
      - q8_0
      - --cache-type-v
      - q8_0
      - --host
      - "0.0.0.0"
      - --port
      - "11346"
      - --temp
      - "0.7"
      - --top-p
      - "1.0"
      - --repeat-penalty
      - "1.0"
      - --jinja
      - -a
      - glm-4.7-flash

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11346/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 600s

    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"

volumes:
  model_cache:
    name: glm47_huggingface_cache
    external: true
